{{- if .Values.podDisruptionBudget.enabled -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "coccinelle-quiz.fullname" . }}
  labels:
    {{- include "coccinelle-quiz.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "coccinelle-quiz.selectorLabels" . | nindent 6 }}
{{- end }}
