apiVersion: v1
kind: Service
metadata:
  name: {{ include "coccinelle-quiz.fullname" . }}
  labels:
    {{- include "coccinelle-quiz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "coccinelle-quiz.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
